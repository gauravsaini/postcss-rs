name: Release & Publish to npm

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  build-matrix:
    name: Build ${{ matrix.target }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-apple-darwin
            os: macos-latest
            use_zig: false
          - target: aarch64-apple-darwin
            os: macos-latest
            use_zig: false
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            use_zig: false
          - target: x86_64-unknown-linux-musl
            os: ubuntu-latest
            use_zig: true
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            use_zig: true
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            use_zig: false
          - target: aarch64-pc-windows-msvc
            os: ubuntu-latest
            use_zig: true
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Setup Zig
        if: ${{ matrix.use_zig }}
        uses: goto-bus-stop/setup-zig@v2

      - name: Cache Cargo dependencies
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Build target binary (Zig)
        if: ${{ matrix.use_zig }}
        run: pnpm exec napi build --platform --target ${{ matrix.target }} --release --zig

      - name: Build target binary (Native)
        if: ${{ !matrix.use_zig }}
        run: pnpm exec napi build --platform --target ${{ matrix.target }} --release

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: bindings-${{ matrix.target }}
          path: '*.node'
          if-no-files-found: error

  publish:
    name: Publish to npm
    needs: build-matrix
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: pnpm install

      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: bindings-*
          merge-multiple: true
          path: .

      - name: List downloaded bindings
        run: ls -la *.node

      - name: Test prebuilt bindings
        run: pnpm test

      - name: Publish to npm
        run: pnpm publish --no-git-checks --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: '*.node'
          generate_release_notes: true
